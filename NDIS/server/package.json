{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:all": "npm run build --workspaces",
    "tsc:build": "tsc --build",
    "start:facade": "npm run dev --workspace=facade"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "tsconfig-paths": "^4.2.0"
  }
}
